# Remielle v1.0.3 修改说明

发布日期：2026-08-21

## 桌宠与待办

- 气泡窗口支持 Todo 右键后直接行内编辑，编辑内容会自动全选，选区颜色为 `#FFB6C1`。
- 完成当天全部任务后显示庆祝提示；删除最后一个未完成任务且剩余任务均已完成时，也会触发同一提示。
- 气泡窗口只显示当天待办；桌宠运行跨过午夜后会自动刷新当天列表。
- 未完成 Todo 跨过午夜后保留未完成状态，并从当天列表归档到历史未完成列表。

## 控制面板

- Todo 页新增“未完成”归档区，按日期折叠分组，交互方式与已完成归档一致。
- 删除 Todo 确认弹窗改为控制面板蓝色配色；取消按钮使用蓝色 1px 描边。
- 恢复 Todo 勾选框的黑色悬停边框。
- 滚动条默认隐藏，滚动时显示更浅的蓝灰色滑块。
- 行内编辑 Todo 时，输入框增加上下内边距以改善文本显示。

## 窗口与更新

- 重复从托盘菜单打开控制面板时会优先聚焦已有窗口，避免创建第二个控制面板实例。
- 优化控制面板单实例、父进程生命周期和已有窗口检测逻辑。
- 控制面板支持手动检查更新，并由桌宠统一执行下载和更新。

## 验证

- `flutter test`：30 项组件测试通过。
- `flutter analyze`：无错误；保留 3 条既有代码风格提示。
